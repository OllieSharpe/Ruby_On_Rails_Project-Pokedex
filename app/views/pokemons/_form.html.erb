<%= form_with(model: pokemon, local: true) do |form| %>
  <% if pokemon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pokemon.errors.count, "error") %> prohibited this pokemon from being saved:</h2>

      <ul>
      <% pokemon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label 'Dex Number:' %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :parent %>
    <%= form.text_field :parent %>
  </div>

  <div class="field">
    <%= form.label :evolution_method %>
    <%= form.text_field :evolution_method %>
  </div>

  <div class="field">
    <%= form.label 'Generation Id:' %>
    <%= form.number_field :generation_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
